<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.tarifarios.mapper.ITarifarioEmisorMapper">
	
	<sql id="consultaVistaMant">
        SELECT id_tarifario_emisor
             , id_institucion
             , descripcion_institucion
             , id_tarifa
             , descripcion_tarifa
             , nivel
             , id_grupo_bin
             , descripcion_grupo_bin
             , rango_inicial
             , rango_final
             , id_clase_transaccion
             , id_codigo_transaccion
             , descripcion_transaccion
             , id_moneda
             , descripcion_moneda
             , tarifa_flat
             , tarifa_porcentual
             , aplica_igv 
        FROM VIE_MAN_TARIFARIO_EMISOR
    </sql>

    <select id="buscarTodos" resultType="TarifarioEmisor" statementType="PREPARED">
       <include refid="consultaVistaMant" />
    </select>

    <select id="buscarTarifarioEmisor" resultType="TarifarioEmisor">
        <include refid="consultaVistaMant" />
            WHERE id_tarifario_emisor = #{idTarifarioEmisor}
    </select>

    <select id="mantener" parameterType="ParametroMantenimiento"
            statementType="CALLABLE">
        {
            CALL PKG_MAN_TARIFARIO.PRC_MAN_TARIFARIO_EMISOR (
                #{modo,                             	jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idTarifarioEmisor,	jdbcType=INTEGER, mode=INOUT},
                #{objetoMantenido.idInstitucion,        jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.idTarifa,         	jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.nivel,				jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.idGrupoBIN,			jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.rangoInicial,			jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.rangoFinal,			jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.idMoneda,				jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.tarifaFlat,			jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.tarifaPorcentual,		jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.aplicaIGV,			jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.idClaseTransaccion,   jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.idCodigoTransaccion,  jdbcType=NUMERIC, mode=IN},
                #{usuario,                          	jdbcType=VARCHAR, mode=IN}
            )
        }
    </select>
</mapper>

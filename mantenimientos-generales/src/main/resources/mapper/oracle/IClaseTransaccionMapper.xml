<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ob.unibanca.sicf.mantenimientosgenerales.mapper.IClaseTransaccionMapper">

	<select id="buscarTodos" resultType="ClaseTransaccion"
            statementType="PREPARED">
        SELECT id_clase_transaccion    AS idclasetransaccion
		     , descripcion             AS descripcionclasetransaccion
		     , tipo_transaccion        AS tipotransaccion
		FROM MAE_CLASE_TRANSACCION
    </select>

    <select id="mantener" parameterType="ParametroMantenimiento"
            statementType="CALLABLE">
        {
            CALL pkg_man_generales.PRC_MAN_CLASE_TRANSACCION(
                #{modo, 								        jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idClaseTransaccion, 	        jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.descripcionClaseTransaccion,  jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.tipoTransaccion,  	        jdbcType=VARCHAR, mode=IN},
                #{usuario, 								        jdbcType=VARCHAR, mode=IN}
            )
        }
    </select>

</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.mantenimientosgenerales.mapper.IServicioMapper">

    <select id="buscarTodos" statementType="PREPARED" resultType="Servicio">
        SELECT id_servicio           AS idservicio
             , descripcion           AS descripcion
             , id_membresia          AS idmembresia
             , descripcion_membresia AS descripcionmembresia
            FROM vie_man_servicio
    </select>

    <select id="buscarUno" resultType="Servicio">
        SELECT id_servicio           AS idservicio
             , descripcion           AS descripcion
             , id_membresia          AS idmembresia
             , descripcion_membresia AS descripcionmembresia
            FROM vie_man_servicio
            WHERE id_servicio = #{idServicio}
              AND id_membresia = #{idMembresia}
    </select>

    <select id="mantener" statementType="CALLABLE" parameterType="ParametroMantenimiento">
        {
            CALL PKG_MAN_GENERALES.prc_man_servicio(
                #{modo, jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idMembresia, jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idServicio, jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.descripcion, jdbcType=VARCHAR, mode=IN},
                #{usuario, jdbcType=VARCHAR, mode=IN}
            )
        }
    </select>

</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.mantenimientosgenerales.mapper.IModoEntradaPOSMapper">

    <sql id="consultaVistaMant">
        SELECT id_membresia
             , id_modo_entrada_pos
             , descripcion_modo_entrada_pos
             , descripcion_membresia
        FROM VIE_MAN_MODO_ENTRADA_POS
    </sql>

    <select id="buscarTodos" resultType="ModoEntradaPOS" statementType="PREPARED">
        <include refid="consultaVistaMant"/>
    </select>

    <select id="buscarModoEntradaPOS" resultType="ModoEntradaPOS">
        <include refid="consultaVistaMant"/>
        WHERE id_membresia = #{idMembresia} AND id_modo_entrada_pos = #{idModoEntradaPOS}
    </select>

    <select id="buscarModosEntradasPOSPorMembresia" resultType="ModoEntradaPOS">
        <include refid="consultaVistaMant"/>
        WHERE id_membresia = #{idMembresia}
    </select>

    <select id="mantener" parameterType="ParametroMantenimiento"
            statementType="CALLABLE">
        {
            CALL PKG_MAN_GENERALES.PRC_MAN_MODO_ENTRADA_POS(
                #{modo,                                 		jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idMembresia,          		jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idModoEntradaPOS,     		jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.descripcionModoEntradaPOS,  	jdbcType=VARCHAR, mode=IN},
                #{usuario,                              		jdbcType=VARCHAR, mode=IN}
            )
        }
    </select>
</mapper>
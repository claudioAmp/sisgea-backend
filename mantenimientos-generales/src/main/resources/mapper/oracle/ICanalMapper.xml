<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.mantenimientosgenerales.mapper.ICanalMapper">

    <select id="buscarTodos" resultType="Canal" statementType="PREPARED">
        SELECT id_canal
             , descripcion      AS descripcion_canal
        FROM MAE_CANAL
    </select>

    <select id="mantener" parameterType="ParametroMantenimiento" statementType="CALLABLE">
        {
            CALL PKG_MAN_GENERALES.PRC_MAN_CANAL(
                #{modo, 							  jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idCanal, 	          jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.descripcionCanal,   jdbcType=VARCHAR, mode=IN},
                #{usuario, 						      jdbcType=VARCHAR, mode=IN}
            )
        }
    </select>

</mapper>

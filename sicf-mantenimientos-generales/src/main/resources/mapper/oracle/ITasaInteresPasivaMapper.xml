<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.mantenimientosgenerales.mapper.ITasaInteresPasivaMapper">

    <sql id="consultaVistaMant">
        SELECT
            fecha_proceso,
            fecha_afect_moneda_base,
            fecha_afect_moneda_alterna,
            tasa_interes_moneda_base,
            tasa_interes_moneda_alterna,
            factor_diario_moneda_base,
            factor_diario_moneda_alterna
        FROM
            VIE_MAN_TASA_INTERES_PASIVA
    </sql>

    <select id="buscarTodos" resultType="TasaInteresPasiva" statementType="PREPARED">
        <include refid="consultaVistaMant"/>
    </select>

    <select id="buscarTasaInteresPasiva" resultType="TasaInteresPasiva">
        <include refid="consultaVistaMant"/>
        WHERE fecha_proceso = #{fechaProceso}
    </select>

    <select id="mantener" parameterType="ParametroMantenimiento"
            statementType="CALLABLE">
        {
            CALL PKG_MAN_GENERALES.PRC_MAN_TASA_INTERES_PASIVA(
                #{modo,                                         jdbcType=VARCHAR , mode=IN    },
                #{objetoMantenido.fechaProceso,                 jdbcType=DATE    , mode=INOUT },
                #{objetoMantenido.fechaAfectMonedaBase,         jdbcType=DATE    , mode=IN    },
                #{objetoMantenido.fechaAfectMonedaAlterna,      jdbcType=DATE    , mode=IN    },
                #{objetoMantenido.tasaInteresMonedaBase,        jdbcType=NUMERIC , mode=IN    },
                #{objetoMantenido.tasaInteresMonedaAlterna,     jdbcType=NUMERIC , mode=IN    },
                #{objetoMantenido.factorDiarioMonedaBase,       jdbcType=NUMERIC , mode=OUT   },
                #{objetoMantenido.factorDiarioMonedaAlterna,    jdbcType=NUMERIC , mode=OUT   },
                #{usuario,                                      jdbcType=VARCHAR , mode=IN    }
            )
        }
    </select>
</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.mantenimientosgenerales.mapper.ICodigoRazonCobroVisaMapper">
    <sql id="consultaVistaMant">
        SELECT
            id_codigo_razon,
            descripcion_codigo_razon,
            id_clase_transaccion,
            id_codigo_transaccion,
            descripcion_transaccion
        FROM
            VIE_MAN_CODIGO_RAZON_COBRO_VISA
    </sql>

    <select id="buscarTodos" resultType="CodigoRazonCobroVisa" statementType="PREPARED">
        <include refid="consultaVistaMant"/>
    </select>

    <select id="buscarCodigoRazonCobroVisa" resultType="CodigoRazonCobroVisa" statementType="PREPARED">
        <include refid="consultaVistaMant"/>
        WHERE id_codigo_razon = #{idCodigoRazon}
    </select>

    <select id="mantener" resultType="ParametroMantenimiento"
            statementType="CALLABLE">
        {
            CALL PKG_MAN_GENERALES.PRC_MAN_CODIGO_RAZON_COBRO_VISA(
                #{modo,                                         jdbcType=VARCHAR , mode=IN    },
                #{objetoMantenido.idCodigoRazon,                jdbcType=VARCHAR , mode=IN    },
                #{objetoMantenido.descripcionCodigoRazon,       jdbcType=VARCHAR , mode=IN    },
                #{objetoMantenido.idClaseTransaccion,           jdbcType=NUMERIC , mode=IN    },
                #{objetoMantenido.idCodigoTransaccion,          jdbcType=NUMERIC , mode=IN    },
                #{usuario,                                      jdbcType=VARCHAR , mode=IN    }
            )
        }
    </select>
</mapper>
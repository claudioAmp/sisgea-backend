<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.mantenimientosgenerales.mapper.IMultitabCabMapper">

    <select id="buscarTodos" resultType="MultitabCab" statementType="PREPARED">
        SELECT id_multitab_cab
             , descripcion      AS descripcion_multitab_cab
        FROM MAE_MULTITAB_CAB
        <include refid="orden"/>
    </select>
    
    <sql id="orden">
    	ORDER BY id_multitab_cab ASC
    </sql>

    <select id="mantener" parameterType="ParametroMantenimiento" statementType="CALLABLE">
        {
            CALL PKG_MAN_GENERALES.PRC_MAN_MULTITAB_CAB(
                #{modo, 								    jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idMultitabCab, 	        jdbcType=INTEGER, mode=IN},
                #{objetoMantenido.descripcionMultitabCab,   jdbcType=VARCHAR, mode=IN},
                #{usuario, 								    jdbcType=VARCHAR, mode=IN}
            )
        }
    </select>

</mapper>

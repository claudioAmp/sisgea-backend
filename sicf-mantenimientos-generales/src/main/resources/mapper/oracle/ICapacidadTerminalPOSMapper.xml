<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.mantenimientosgenerales.mapper.ICapacidadTerminalPOSMapper">

    <sql id="consultaVistaMant">
        SELECT id_membresia
             , id_terminal_pos
             , descripcion_terminal_pos
             , descripcion_membresia
        FROM VIE_MAN_CAPACIDAD_TERM_POS
    </sql>
    
    <sql id="orden">
    	ORDER BY id_terminal_pos ASC
    </sql>

    <select id="buscarTodos" resultType="CapacidadTerminalPOS" statementType="PREPARED">
        <include refid="consultaVistaMant"/>
        <include refid="orden"/>
    </select>

    <select id="buscarCapacidadTerminalPOS" resultType="CapacidadTerminalPOS">
        <include refid="consultaVistaMant"/>
        WHERE id_membresia = #{idMembresia} AND id_terminal_pos = #{idTerminalPOS}
    </select>

    <select id="buscarCapacidadesTerminalesPOSPorMembresia" resultType="CapacidadTerminalPOS">
        <include refid="consultaVistaMant"/>
        WHERE id_membresia = #{idMembresia}
    </select>

    <select id="mantener" parameterType="ParametroMantenimiento"
            statementType="CALLABLE">
        {
            CALL PKG_MAN_GENERALES.PRC_MAN_CAPACIDAD_TERM_POS(
                #{modo,                                 	jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idMembresia,          	jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idTerminalPOS,     		jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.descripcionTerminalPOS,	jdbcType=VARCHAR, mode=IN},
                #{usuario,                              	jdbcType=VARCHAR, mode=IN}
            )
        }
    </select>
    
</mapper>
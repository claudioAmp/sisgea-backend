<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.mantenimientosgenerales.mapper.IEventosTransaccionVisaMapper">
	
	<sql id="consulta">
        SELECT * 
        FROM VIE_MAN_BILLING_LINE_VISA_X_TXN
    </sql>
        
    <select id="buscarTodos" resultType="EventosTransaccionVisa" statementType="PREPARED">
        <include refid="consulta"/>
    </select>
    
     <select id="buscarUno" resultType="EventosTransaccionVisa" statementType="PREPARED">
        <include refid="consulta"/>
        <where>
        	<if test="objetoMantenido.idBillingLineVisa">
        		ID_BILLING_LINE_VISA = #{objetoMantenido.idBillingLineVisa}
        	</if>
        </where>
    </select>
    
    <select id="mantener" parameterType="ParametroMantenimiento" statementType="CALLABLE">
        {
            CALL PKG_MAN_GENERALES.PRC_MAN_BILLING_LINE_VISA_X_TXN(
                #{modo, 							  	jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idBillingLineVisa,  	jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.serviceDescription,  	jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idClaseTransaccion,   jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.idCodigoTransaccion,  jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.indAlicuota,   		jdbcType=NUMERIC, mode=IN}
            )
        }
    </select>
    
</mapper>

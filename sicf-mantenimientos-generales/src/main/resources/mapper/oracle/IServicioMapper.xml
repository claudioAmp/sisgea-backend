<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.mantenimientosgenerales.mapper.IServicioMapper">

    <sql id="consultaVistaMant">
        SELECT id_membresia
             , id_servicio
             , descripcion_servicio
             , descripcion_membresia
        FROM VIE_MAN_SERVICIO
    </sql>
    
    <sql id="orden">
    	ORDER BY id_membresia ASC,id_servicio ASC
    </sql>

    <select id="buscarTodos" resultType="Servicio" statementType="PREPARED">
        <include refid="consultaVistaMant"/>
        <include refid="orden"/>
    </select>

    <select id="buscarServicio" resultType="Servicio">
        <include refid="consultaVistaMant"/>
        WHERE id_membresia = #{idMembresia} AND id_servicio = #{idServicio}
    </select>

    <select id="buscarServiciosPorMembresia" resultType="Servicio">
        <include refid="consultaVistaMant"/>
        WHERE id_membresia = #{idMembresia}
    </select>

    <select id="mantener" parameterType="ParametroMantenimiento"
            statementType="CALLABLE">
        {
            CALL PKG_MAN_GENERALES.PRC_MAN_SERVICIO(
                #{modo,                                 jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idMembresia,          jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idServicio,           jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.descripcionServicio,  jdbcType=NUMERIC, mode=IN},
                #{usuario,                              jdbcType=VARCHAR, mode=IN}
            )
        }
    </select>
</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.mantenimientosgenerales.mapper.IProductoMapper">

    <select id="buscarTodos" resultType="Producto" statementType="PREPARED">
        SELECT id_producto
        , descripcion      AS descripcion_producto
        FROM MAE_PRODUCTO
        <include refid="orden"/>
    </select>

    <sql id="orden">
        ORDER BY id_producto
    </sql>

    <select id="mantener" parameterType="ParametroMantenimiento" statementType="CALLABLE">
        {
        CALL PKG_MAN_GENERALES.PRC_MAN_PRODUCTO(
        #{modo, 							      jdbcType=VARCHAR, mode=IN},
        #{objetoMantenido.idProducto, 	          jdbcType=NUMERIC, mode=IN},
        #{objetoMantenido.descripcionProducto,    jdbcType=VARCHAR, mode=IN},
        #{usuario, 						          jdbcType=VARCHAR, mode=IN}
        )
        }
    </select>

</mapper>


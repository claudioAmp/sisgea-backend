<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.conciliacion.mapper.ICampoInsertarObservadaMapper">

    <sql id="consultaVistaMant">
        SELECT id_cmp_ins_observada
        	 , id_conc_tabla
        	 , nombre_procedimiento
        	 , campo_txn_observada
             , campo_insertar
        	 , id_origen_archivo
             , descripcion_origen_archivo
        FROM VIE_MAN_CNC_INSERTAR_OBSERVADA
    </sql>
    
    <sql id="orden">
    	ORDER BY id_conc_tabla, id_cmp_ins_observada
    </sql>

    <select id="buscarTodos" resultType="CampoInsertarObservada" statementType="PREPARED">
        <include refid="consultaVistaMant"/>
        <include refid="orden"/>
    </select>

    <select id="buscarCampoInsertarObservada" resultType="CampoInsertarObservada">
        <include refid="consultaVistaMant"/>
        WHERE
                id_cmp_ins_observada  = #{idCampoInsertarObservada}
            AND id_conc_tabla = #{idConcTabla}
    </select>

    <select id="buscarCampoInsertarObservadaPorConciliacionTablas" resultType="CampoInsertarObservada">
        <include refid="consultaVistaMant"/>
        WHERE
            id_conc_tabla = #{idConcTabla}
    </select>

    <select id="mantener" parameterType="ParametroMantenimiento" statementType="CALLABLE">
        {
            CALL PKG_MAN_CONCILIACION.PRC_MAN_CAMPO_INSERTAR_OBSERVADA(
                #{modo, 								jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idCmpInsObservada,	jdbcType=INTEGER, mode=INOUT},
                #{objetoMantenido.idConcTabla, 	  		jdbcType=INTEGER, mode=IN},
                #{objetoMantenido.campo_txn_observada,	jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.campo_insertar,		jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.id_origen_archivo,	jdbcType=VARCHAR, mode=IN},
                #{usuario, 								jdbcType=VARCHAR, mode=IN}
            )
        }
    </select>

</mapper>

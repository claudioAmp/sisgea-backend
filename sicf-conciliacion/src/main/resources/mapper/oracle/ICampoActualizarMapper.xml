<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.conciliacion.mapper.ICampoActualizarMapper">

    <sql id="consultaVistaMant">
        SELECT id_campo_actualizar
        	 , id_conc_tabla
        	 , nombre_procedimiento
        	 , id_tabla_campo_act
             , descripcion_tabla_campo_act
        	 , id_tabla_campo_ref
             , descripcion_tabla_campo_ref
             , campo_actualizar
             , campo_referencia
        FROM VIE_MAN_CNC_CAMPO_ACTUALIZAR
    </sql>
    
    <sql id="orden">
    	ORDER BY id_conc_tabla, id_campo_actualizar
    </sql>

    <select id="buscarTodos" resultType="CampoActualizar" statementType="PREPARED">
        <include refid="consultaVistaMant"/>
        <include refid="orden"/>
    </select>

    <select id="buscarCampoActualizar" resultType="CampoActualizar">
        <include refid="consultaVistaMant"/>
        WHERE
                id_campo_actualizar  = #{idCampoActualizar}
            AND id_conc_tabla = #{idConcTabla}
    </select>

    <select id="buscarCampoActualizarPorConciliacionTablas" resultType="CampoActualizar">
        <include refid="consultaVistaMant"/>
        WHERE
            id_conc_tabla = #{idConcTabla}
    </select>

    <select id="mantener" parameterType="ParametroMantenimiento" statementType="CALLABLE">
        {
            CALL PKG_MAN_CONCILIACION.PRC_MAN_CAMPO_ACTUALIZAR(
                #{modo, 								jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idCampoActualizar,	jdbcType=INTEGER, mode=INOUT},
                #{objetoMantenido.idConcTabla, 	  		jdbcType=INTEGER, mode=IN},
                #{objetoMantenido.idTablaCampoAct,		jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idTablaCampoRef,		jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.campoActualizar,		jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.campoReferencia,		jdbcType=VARCHAR, mode=IN},
                #{usuario, 								jdbcType=VARCHAR, mode=IN}
            )
        }
    </select>

</mapper>

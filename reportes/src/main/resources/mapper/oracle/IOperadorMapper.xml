<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.reportes.mapper.IOperadorMapper">

    <!--select id="buscarOperadorPorTipoDato" resultType="Operador">
        SELECT * FROM MAE_OPERADOR
        WHERE ID_OPERADOR IN
        (
            SELECT ID_OPERADOR FROM MAE_OPERADOR_TIPODATO
            WHERE
                ID_TIPO_DATO = #{tipoDato}
        )
    </select-->

</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.generadorconsultas.mapper.IUsuarioPerfilMapper">

    <sql id="consultaVistaMant">
       SELECT
       	  ID_PERFIL_REP_USUARIO			AS ID_PERFIL_REP_USUARIO,
		  ID_PERFIL						AS ID_PERFIL,
		  DESCRIPCION_PERFIL			AS PERFIL,
		  USUARIO						AS USUARIO
		FROM VIE_MAN_REP_PERFIL_USUARIO 
    </sql>

     <select id="buscarTodos" resultType="UsuarioPerfil"
            statementType="PREPARED">
        <include refid="consultaVistaMant"/>
    </select>
    
    <select id="buscarPorCriterios" resultType="UsuarioPerfil" parameterType="CriterioBusquedaUsuarioPerfil"
            statementType="PREPARED">
        <include refid="consultaVistaMant"/>
        <where>
	        <if  test="idPerfil != null and  idPerfil != ''">
	       		AND ID_PERFIL = #{idPerfil}
	        </if>
	         <if  test="usuario != null and  usuario != ''">
       			AND USUARIO = #{usuario}
        	</if>
        </where>
       
        
    </select>
    
    <select id="mantener" parameterType="ParametroMantenimiento" statementType="CALLABLE">
        {
        	CALL PKG_MAN_REP.prc_man_rep_perfil_usuario 	(
      			#{modo, 							  			jdbcType=VARCHAR, mode=IN},
      			#{objetoMantenido.idPerfilRepUsuario, 	     	jdbcType=INTEGER, mode=INOUT},
                #{objetoMantenido.usuario,   	  				jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idPerfil, 	          		jdbcType=NUMERIC, mode=IN},
                #{usuario, 						      			jdbcType=VARCHAR, mode=IN}
                )
        }
    </select>

</mapper>

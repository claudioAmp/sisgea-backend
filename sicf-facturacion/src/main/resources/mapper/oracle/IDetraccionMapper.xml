<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.facturacion.mapper.IDetraccionMapper">

    <select id="buscarTodos" resultType="Detraccion" statementType="PREPARED">
        SELECT id_detraccion
             , descripcion AS descripcionDetraccion
             , porcentaje_detraccion
        FROM MAE_DETRACCION
    </select>

    <select id="mantener" parameterType="ParametroMantenimiento" statementType="CALLABLE">
        {
            CALL PKG_MAN_GENERALES.PRC_MAN_DETRACCION(
                #{modo									,jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idDetraccion			,jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.descripcionDetraccion ,jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.porcentajeDetraccion  ,jdbcType=NUMERIC, mode=IN},
                #{usuario 								,jdbcType=VARCHAR, mode=IN}
            )
        }
    </select>
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.facturacion.mapper.ISecuenciaEmisionMapper">

    <select id="buscarTodos" resultType="SecuenciaEmision" statementType="PREPARED">
        SELECT id_secuencia_emision,
               origen_solicitud,
               tipo_documento,
               descripcion_documento,
               numero_serie,
               secuencia
        FROM MAE_SECUENCIA_EMISION
    </select>

    <select id="buscarSecuenciaEmision" resultType="SecuenciaEmision" statementType="PREPARED">
        SELECT id_secuencia_emision,
               origen_solicitud,
               tipo_documento,
               descripcion_documento,
               numero_serie,
               secuencia
        FROM MAE_SECUENCIA_EMISION
        WHERE id_secuencia_emision = #{idSecuenciaEmision}
    </select>

    <select id="mantener" parameterType="ParametroMantenimiento" statementType="CALLABLE">
        {
            CALL PKG_MAN_GENERALES.PRC_MAN_SECUENCIA_EMISION(
                #{modo,                                  jdbcType=VARCHAR, mode=IN       },
                #{objetoMantenido.idSecuenciaEmision,    jdbcType=INTEGER, mode=INOUT    },
                #{objetoMantenido.origenSolicitud,       jdbcType=INTEGER, mode=IN       },
                #{objetoMantenido.tipoDocumento,         jdbcType=VARCHAR, mode=IN       },
                #{objetoMantenido.descripcionDocumento,  jdbcType=VARCHAR, mode=IN       },
                #{objetoMantenido.numeroSerie,           jdbcType=VARCHAR, mode=IN       },
                #{objetoMantenido.secuencia,             jdbcType=INTEGER, mode=IN       }
            )
        }
    </select>
</mapper>
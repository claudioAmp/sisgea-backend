<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.facturacion.mapper.ITipoNotaMapper">

    <select id="buscarTodos" resultType="TipoNota" statementType="PREPARED">
        SELECT id_origen_nota
        	 , id_nota
             , descripcion AS descripcionTipoNota
        FROM MAE_TIPO_NOTA
    </select>

    <select id="mantener" parameterType="ParametroMantenimiento" statementType="CALLABLE">
        {
            CALL PKG_MAN_GENERALES.PRC_MAN_TIPO_NOTA(
                #{modo									,jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idOrigenNota			,jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.idNota				,jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.descripcionDetraccion ,jdbcType=VARCHAR, mode=IN},
                #{usuario 								,jdbcType=VARCHAR, mode=IN}
            )
        }
    </select>
</mapper>

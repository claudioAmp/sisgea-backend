<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.facturacion.mapper.ICodigoFacturacionMapper">

    <select id="buscarTodos" resultType="CodigoFacturacion" statementType="PREPARED">
        SELECT *
        FROM VIE_MAN_CODIGO_FACTURACION
    </select>

    <select id="mantener" parameterType="ParametroMantenimiento" statementType="CALLABLE">
        {
            CALL PKG_MAN_GENERALES.PRC_MAN_CODIGO_FACTURACION(
                #{modo, jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idCodigoFacturacion, jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.descIdCodigoFacturacion, jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.indTipoFacturacion, jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.idDetraccion, jdbcType= NUMERIC, mode=IN}
                #{usuario, jdbcType=VARCHAR, mode=IN}
            )
        }
    </select>
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.facturacion.mapper.ITipoDocumentoMapper">

    <select id="buscarTodos" resultType="TipoNota" statementType="PREPARED">
        SELECT id_documento
             , descripcion AS descripcionTipoDocumento
        FROM MAE_TIPO_DOCUMENTO
    </select>

    <select id="mantener" parameterType="ParametroMantenimiento" statementType="CALLABLE">
        {
            CALL PKG_MAN_GENERALES.PRC_MAN_TIPO_DOCUMENTO(
                #{modo										 ,jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idDocumento				 ,jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.descripcionTipoDocumento   ,jdbcType=VARCHAR, mode=IN},
                #{usuario 									 ,jdbcType=VARCHAR, mode=IN}
            )
        }
    </select>
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.facturacion.mapper.IPortafolioMapper">

    <select id="buscarTodos" resultType="Portafolio" statementType="PREPARED">
        SELECT id_portafolio
             , descripcion   AS descripcion_portafolio
            FROM mae_portafolio
    </select>

    <select id="mantener" parameterType="ParametroMantenimiento" statementType="CALLABLE">
        {
            CALL pkg_man_generales.PRC_MAN_PORTAFOLIO(
                #{modo, jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idPortafolio, jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.descripcion, jdbcType=VARCHAR, mode=IN},
                #{usuario, jdbcType=VARCHAR, mode=IN}
            )
        }
    </select>
</mapper>

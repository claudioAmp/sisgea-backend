<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ob.unibanca.sicf.facturacion.mapper.ICargaCobrosMiscelaneosMapper">

	<select id="cargarRegistro" statementType="CALLABLE" parameterType="java.util.Map">
		{
		call ${procedureName}
			(
				#{salida, jdbcType = VARCHAR, mode = OUT},
				<foreach collection="params" index="key" item="value" open="" separator="," close="">
						#{value, jdbcType = VARCHAR, mode = IN}
				</foreach>,
				#{filename, jdbcType = VARCHAR, mode = OUT},
				#{reproceso, jdbcType = VARCHAR, mode = OUT}
			)
		}
	</select>

</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.consultasgenerales.mapper.ITipoCambioMembMapper">
	
	<sql id="consulta">
		SELECT *
		FROM VIE_CON_MOV_TIPO_CAMBIO_MEMB
	</sql>
	
	<sql id="orden">
		ORDER BY 
        	fecha_tipo_cambio DESC, 
        	id_membresia ASC
	</sql>
	
	<select id="buscarTodos" resultType="TipoCambioMemb" statementType="PREPARED">
        <include refid="consulta"/>
        <include refid="orden"/>
    </select>
    
    <select id="buscarPorIdTipoCambio" resultType="TipoCambioMemb" statementType="PREPARED">
        <include refid="consulta"/>
        <where>
        	<if test="idMembresia != null">
        		fecha_tipo_cambio = (SELECT FECHA_PROCESO FROM MAE_PARAMETRO_SISTEMA)
        		AND id_membresia = #{idMembresia}
        	</if>
        </where>        
    </select>
    
    
    <select id="mantener" parameterType="ParametroMantenimiento"
            statementType="CALLABLE">
        {
            CALL PKG_CONSULTAS.PRC_CON_TIPO_CAMBIO_MEMBRESIA(
                #{modo,                                 jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idMembresia,          jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.tipoCambio, 			jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.fechaTipoCambio, 		jdbcType=DATE, 	  mode=IN},
                #{usuario,                              jdbcType=VARCHAR, mode=IN}
            )
        }
    </select>

</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="edu.taller.sisgea.tarifarios.mapper.ITipoTarifaMapper">
	 
	<sql id="consultaVistaMant">
        SELECT id_tarifa
             , descripcion AS descripcion_tipo_tarifa
             , aplica_grupo_bin
             , aplica_dif_txn
             , id_esquema
             , descripcion_esquema
        FROM VIE_MAN_TIPO_TARIFA
    </sql>
    
    <sql id="orderBy">
    	ORDER BY id_tarifa
    </sql>    

    <select id="buscarTodos" resultType="TipoTarifa" statementType="PREPARED">
       <include refid="consultaVistaMant" />
       <include refid="orderBy" />
    </select>

    <select id="buscarTipoTarifa" resultType="TipoTarifa">
        <include refid="consultaVistaMant" />
            WHERE id_tarifa = #{idTarifa}
    </select>

    <select id="mantener" parameterType="ParametroMantenimiento"
            statementType="CALLABLE">
        {
            CALL PKG_MAN_TARIFARIO.PRC_MAN_TIPO_TARIFA(
                #{modo,                             		jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idTarifa,         		jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.descripcionTipoTarifa,	jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.aplicaGrupoBIN,			jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.aplicaDifTxn,				jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.idEsquema,				jdbcType=NUMERIC, mode=IN},
                #{usuario,                          		jdbcType=VARCHAR, mode=IN}
            )
        }
    </select>
</mapper>

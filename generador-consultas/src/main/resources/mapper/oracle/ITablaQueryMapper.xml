<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.generadorconsultas.mapper.ITablaQueryMapper">

	<sql id="consultaVistaMant">
       SELECT 
		  *
        FROM
		VIE_MAN_REP_TABLA_QUERY
    </sql>

     <select id="buscarTodos" resultType="TablaQuery" statementType="PREPARED">
        <include refid="consultaVistaMant"/>
    </select>
	<select id="buscarPorCriterios" resultType="TablaQuery" parameterType="CriterioBusquedaTablaQuery" statementType="PREPARED">
        <include refid="consultaVistaMant"/>
        <if  test="idTablaQuery!= null and idTablaQuery != ''">
       			WHERE
       			ID_TABLA_QUERY =#{idTablaQuery}
        </if>
        <if  test="idReporte!= null and idReporte != ''">
       			WHERE
       			ID_REPORTE =#{idReporte}
        </if>
        <if  test="idTabla!= null and idTabla != ''">
       			WHERE
       			ID_TABLA =#{idTabla}
        </if>
        
        <if  test="idTabla!= null and idTabla != '' and idReporte!= null and idReporte != '' and idTablaQuery!= null and idTablaQuery != ''">
        	WHERE
        	    ID_TABLA_QUERY =#{idTablaQuery}
       			AND ID_TABLA =#{idTabla}
       			AND ID_REPORTE =#{idReporte}
        </if>
    </select>
   
    
     <select id="mantener" parameterType="ParametroMantenimiento" statementType="CALLABLE">
        {
            CALL PKG_MAN_REP.prc_man_rep_tabla_query(
                #{modo, 						  jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idTablaQuery,   jdbcType=NUMERIC, mode=INOUT},
                #{objetoMantenido.idReporte,      jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.idTabla,  	  jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.idInstancia,    jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.orden , 	      jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.esBase , 	      jdbcType=NUMERIC, mode=IN},
                #{usuario, 						  jdbcType=VARCHAR, mode=IN}
            )
        }
    </select>
    
</mapper>

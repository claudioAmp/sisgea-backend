<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.generadorconsultas.mapper.IPerfilMapper">


    <select id="buscarTodos" resultType="Perfil" statementType="PREPARED">
         SELECT 
		  ID_PERFIL_REP     AS ID_PERFIL,
		  DESCRIPCION   	AS DESCRIPCION,
		  ACTIVO  		 	AS ACTIVO
		FROM MAE_PERFIL_REP
    </select>
    
     <select id="mantener" parameterType="ParametroMantenimiento" statementType="CALLABLE">
        {
            CALL PKG_MAN_REP.prc_man_rep_perfil(
                #{modo, 							  jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idPerfil, 	      jdbcType=INTEGER, mode=IN},
                #{objetoMantenido.descripcion,   	  jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.activo, 	          jdbcType=NUMERIC, mode=IN},
                #{usuario, 						      jdbcType=VARCHAR, mode=IN}
            )
        }
    </select>
    
</mapper>

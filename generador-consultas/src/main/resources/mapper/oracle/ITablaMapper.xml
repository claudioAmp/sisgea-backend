<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.generadorconsultas.mapper.ITablaMapper">


    <sql id="consultaVistaMant">
       SELECT 
		  ID_TABLA      AS ID_TABLA,
		  NOMBRE        AS NOMBRE,
		  DESCRIPCION   AS DESCRIPCION,
		  DESC_CORTA    AS DESC_CORTA,
		  ALIAS         AS ALIAS,
		  ID_TIPO_TABLA AS ID_TIPO_TABLA,
		  TIPO_TABLA	AS TIPO_TABLA
		FROM VIE_MAN_REP_TABLA
    </sql>
    
     <select id="buscarTodos" resultType="Tabla" statementType="PREPARED">
        <include refid="consultaVistaMant"/>
    </select>
	<select id="buscarPorCriterios" resultType="Tabla" parameterType="CriterioBusquedaTabla" statementType="PREPARED">
        <include refid="consultaVistaMant"/>
        <if  test="nombre != null and nombre != ''">
        	WHERE
       			NOMBRE = #{nombre}
        </if>
        <if  test="idTipoTabla!= null and idTipoTabla != ''">
        	WHERE
       			ID_TIPO_TABLA = #{idTipoTabla}
        </if>
        
    </select>
   
    
     <select id="mantener" parameterType="ParametroMantenimiento" statementType="CALLABLE">
        {
            CALL PKG_MAN_REP.prc_man_rep_tabla(
                #{modo, 							      jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idTabla,       jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.nombre,  	 	  jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.alias , 	      jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.descripcion, 	  jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.desCorta, 	  jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idTipoTabla , jdbcType=VARCHAR, mode=IN},
                #{usuario, 						  jdbcType=VARCHAR, mode=IN}
            )
        }
    </select>
    
</mapper>

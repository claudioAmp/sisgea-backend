<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.generadorconsultas.mapper.IFiltroMapper">

	<sql id="consultaVistaMant">
       SELECT 
            *
		FROM VIE_MAN_REP_FILTRO_CAMPO
	</sql>
     <select id="buscarTodos" resultType="Filtro" statementType="PREPARED">
        <include refid="consultaVistaMant"/>
    </select>
    <select id="buscarPorCriterios" resultType="Filtro" parameterType="CriterioBusquedaFiltro" statementType="PREPARED">
        <include refid="consultaVistaMant"/>
        
        <if  test=" idFiltroCampo!= null and idFiltroCampo != ''">
       			WHERE
       			ID_FILTRO_CAMPO =#{idFiltroCampo}
        </if>
        <if  test=" idCampoQuery!= null and idCampoQuery != '' ">
       			WHERE
       			ID_CAMPO_QUERY =#{idCampoQuery}
        </if>
        <if  test=" (idTabla== null and idTabla == '') and (idReporte!= null and idReporte != '') ">
       			WHERE
       			ID_REPORTE =#{idReporte}
        </if>
        <if  test=" (idTabla!= null and idTabla != '') and (idReporte== null and idReporte == '') ">
       			WHERE
       			ID_TABLA =#{idTabla}
        </if>
        
        <if  test="(idTabla!= null and idTabla != '') and (idReporte!= null and idReporte != '') ">
        	WHERE
        	    ID_REPORTE =#{idReporte}
       			AND ID_TABLA =#{idTabla}
        </if>
    </select>
   
    
     <select id="mantener" parameterType="ParametroMantenimiento" statementType="CALLABLE">
        {
            CALL PKG_MAN_REP.prc_man_rep_filtro_campo_query(
                #{modo, 						  jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idFiltroCampo,  jdbcType=NUMERIC, mode=INOUT},
                #{objetoMantenido.idCampoQuery,   jdbcType=NUMERIC, mode=IN},,
                #{objetoMantenido.idOperador,     jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.valor,     	  jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.operadorLogico, jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.tipoFiltro,     jdbcType=NUMERIC, mode=IN},
                #{usuario, 						  jdbcType=VARCHAR, mode=IN}
            )
        }
    </select>
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ob.unibanca.sicf.tarifarios.mapper.IEscenarioTarifaMapper">
	
	<sql id="consultaVistaMant">
        SELECT id_escenario_tarifa
             , id_institucion
             , descripcion_institucion
             , id_membresia
             , descripcion_membresia
             , id_servicio
             , descripcion_servicio
             , id_origen
             , descripcion_origen
             , id_clase_transaccion
             , descripcion_clase_transaccion
             , id_codigo_transaccion
             , descripcion_codigo_transaccion
             , id_tarifa
             , descripcion_tarifa
        FROM VIE_MAN_ESCENARIO_TARIFA
    </sql>

    <select id="buscarTodos" resultType="EscenarioTarifa" statementType="PREPARED">
       <include refid="consultaVistaMant" />
    </select>

    <select id="buscarEscenarioTarifa" resultType="EscenarioTarifa">
        <include refid="consultaVistaMant" />
            WHERE id_escenario_tarifa = #{idEscenarioTarifa}
    </select>

    <select id="mantener" parameterType="ParametroMantenimiento"
            statementType="CALLABLE">
        {
            CALL PKG_MAN_TARIFARIO.PRC_MAN_ESCENARIO_TARIFA(
                #{modo,                             	jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idEscenarioTarifa,	jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.idIntitucion,   		jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.idServicio,   		jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idMembresia,   		jdbcType=VARCHAR, mode=IN},
                #{objetoMantenido.idOrigen,				jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.idClaseTransaccion,	jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.idCodigoTransaccion,	jdbcType=NUMERIC, mode=IN},
                #{objetoMantenido.idTarifa,				jdbcType=NUMERIC, mode=IN},
                #{usuario,                          	jdbcType=VARCHAR, mode=IN}
            )
        }
    </select>

</mapper>
